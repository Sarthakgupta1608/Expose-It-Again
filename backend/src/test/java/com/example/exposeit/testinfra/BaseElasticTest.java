package com.example.exposeit.testinfra;

/**
 * BaseElasticTest
 *
 * Base class for integration tests validating Elasticsearch document indexing and search.
 * Extends BaseIntegrationTest to inherit the containerized JVM registry and dynamic property binds.
 */
public abstract class BaseElasticTest extends BaseIntegrationTest {
}
